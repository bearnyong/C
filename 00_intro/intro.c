// 전처리기
// 헤더파일 포함
#include <stdio.h>
#include <math.h>

#define PI 3.141592

// C언어 프로그래밍의 시작은 main으로부터!
int main() { //함수 블록 시작
    
    // 변수 선언, 모든 코드는 ;로 끝난다.
    int i;

    // 데이터를 화면에 출력한다. 출력 함수!
    printf("Hello Dokdo!\n");

    // 반복 루프 연습
    for(i=0; i<=10; i++) {
        printf("안녕 나는 반복분!!\n");
    }

    // 데이터 반환
    return 0;
}